echo "# Food Ordering App

## Setup
1. Clone repository
2. Copy \`backend/.env.example\` to \`backend/.env\`
3. Copy \`frontend/.env.example\` to \`frontend/.env\`
4. Fill in your values in .env files
5. Run \`npm install\` in both folders
6. Run \`npm start\` in both folders

## Environment Variables
Check \`.env.example\` files for required variables" > README.md

git add README.md
git commit -m "Add README"
git push
